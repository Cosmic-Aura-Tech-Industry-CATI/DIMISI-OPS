/**
 * Brand assets.
 *
 * Logos live in `public/assets/` so they are served as static files by any
 * bundler/host (Vite dev, production build, Vercel, GitHub export) without
 * relying on hashed module imports or environment-specific asset URLs.
 */
export const BRAND_MARK_SRC = "/assets/dimisi-mark.png";
export const BRAND_LOGO_SRC = "/assets/dimisi-logo.png";
export const BRAND_NAME = "Dimisi Technologies";
